import { creategrowlixaConfig } from "growlixa-agent-playwright-config/config";

export default creategrowlixaConfig({
  // Add your custom playwright configuration overrides here
  // Example:
  // timeout: 60000,
  // use: {
  //   baseURL: 'http://localhost:3000',
  // },
});
